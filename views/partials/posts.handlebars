{{#each posts}}
    <div class="card text-center">
    <div class="card-header">
        Featured Post 
    </div>

    <div class="card-body">
        <h5 class="card-title"><a href="/post/{{id}}" class="link-danger link-underline link-underline-opacity-0">{{title}}</a></h5>
        <p class="card-text">{{content}}</p>
    </div>
    <div class="card-footer text-body-secondary">
    Posted on: {{format_date createdAt}}
    </div>
    </div>
{{/each}}